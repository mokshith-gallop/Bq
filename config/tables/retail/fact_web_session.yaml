# Manifest: retail.fact_web_session
source:
  database: retail
  table: fact_web_session
  cluster: acme-analytics
  format: parquet
  gcs_path: gs://acme-migration-staging-us/retail/fact_web_session/
  partition_cols: [event_date, country]

target:
  project: acme-analytics
  dataset: retail
  table: fact_web_session

transforms:
  partition_key_conversion: null  # event_date is native DATE — no conversion
  map_to_json: []
  type_widening: []

validation:
  watermark_col: null
  null_check_cols: [user_id]

wave: wave_2_medium
migration_tier: critical
