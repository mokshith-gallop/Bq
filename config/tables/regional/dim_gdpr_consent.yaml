# Manifest: regional.dim_gdpr_consent
# GDPR compliance-critical: granted BOOLEAN must not be NULL

source:
  database: regional
  table: dim_gdpr_consent
  cluster: acme-edge
  format: parquet
  gcs_path: gs://acme-migration-staging-eu/regional/dim_gdpr_consent/
  partition_cols:
  - consent_date
target:
  project: acme-analytics-eu
  dataset: regional_eu
  table: dim_gdpr_consent
transforms:
  partition_key_conversion: null
  map_to_json: []
  type_widening: []
wave: eu
validation:
  watermark_col: consent_date
  null_check_cols:
  - consent_id
  - customer_id
  - granted
