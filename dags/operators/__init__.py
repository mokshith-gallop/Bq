# Custom Airflow operators for inline validation of migrated tables.
#
# - inline_validation: Generates BigQueryCheckOperator tasks for row count,
#   partition count, and null key validation checks.
